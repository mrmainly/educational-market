import { AuthLayout, RegistrationForm } from "@/components";

export const RegistrationPage = () => {
    return (
        <AuthLayout>
            <RegistrationForm />
        </AuthLayout>
    );
};
